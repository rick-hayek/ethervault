{
    "name": "@ethervault/core",
    "version": "0.0.1",
    "private": true,
    "main": "src/index.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc -w",
        "type-check": "tsc --noEmit",
        "test": "vitest"
    },
    "devDependencies": {
        "@types/libsodium-wrappers": "^0.7.14",
        "typescript": "^5.0.0",
        "vitest": "^1.0.0"
    },
    "dependencies": {
        "libsodium-wrappers-sumo": "^0.8.1"
    }
}